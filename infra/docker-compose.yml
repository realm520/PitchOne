version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sports
    ports: ["5432:5432"]
  grafana:
    image: grafana/grafana:10.2.0
    ports: ["3000:3000"]
    depends_on: [postgres]
